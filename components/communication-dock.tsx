"use client";
import { useEffect, useState } from "react";
import { ChatPanel } from "./chat-panel";
import styles from "./communication-dock.module.css";
import { communicationRepository } from "@/lib/communication-repository";
import { supabase } from "@/lib/supabase";
import { useBrowserNotifications } from "@/hooks/use-browser-notifications";
export function CommunicationDock(){const [open,setOpen]=useState(false),[unread,setUnread]=useState(0),[availability,setAvailability]=useState<"available"|"busy">("busy"),[support,setSupport]=useState(false);const {permission,enable}=useBrowserNotifications();useEffect(()=>{void(async()=>{const user=(await supabase?.auth.getUser())?.data.user;if(!user)return;const profile=await supabase!.from("profiles").select("role").eq("id",user.id).single();const isSupport=profile.data?.role==="support_rep"||profile.data?.role==="super_admin";setSupport(isSupport);if(isSupport)setAvailability((await communicationRepository.availability()) as "available"|"busy");setUnread(await communicationRepository.unreadCount());})();},[]);const toggle=async()=>{if(!open&&permission==="default")await enable();setOpen(!open);};return <div className={styles.dock}>{open&&<div className={styles.panel}>{support&&<button className="outline mini" onClick={async()=>{const next=availability==="available"?"busy":"available";await communicationRepository.setAvailability(next);setAvailability(next);}}>{availability==="available"?"Available":"Busy"}</button>}<ChatPanel compact onMinimize={()=>setOpen(false)}/></div>}<button className={styles.launcher} onClick={toggle} aria-label={open?"Minimize messages":"Open messages"}>{unread>0&&<small>{unread}</small>}{open?"-":"M"}</button></div>}
